/* playProjectVideo = (video) => {
    video.map(item => {
        item.play();
        item.onended = () => item.play()
    })
}

const video = [...document.querySelectorAll('.video__gif')];
const vid = new Video

playProjectVideo(video) */